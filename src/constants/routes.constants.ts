export enum SWITCH_ROUTES {
  AUTH = 'switch/auth',
  USER = 'switch/user',
}

export enum AUTH_ROUTES {
  SIGN_IN = 'auth/sign-in',
  SIGN_UP = 'auth/sign-up',
}

export enum USER_ROUTES {
  HOME = 'user/home',
}
